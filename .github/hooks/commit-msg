#!/bin/bash
# Asegura que el mensaje de commit comience con un número de ticket (e.g., ABC-123)
if ! grep -q '^[A-Z]*-[0-9]*' "$1"; then
  echo "Error: El mensaje de commit debe comenzar con un número de ticket (por ejemplo, ABC-123)"
  exit 1
fi
